# MarketRadar 快速启动指南

## 🚀 一键启动

**最简单的方式：**
```
双击 start.bat
```

启动后会看到菜单，选择 `[3] Start Web Dashboard` 即可打开可视化界面。

---

## 📊 Web Dashboard（推荐）

### 启动方式
```bash
streamlit run pipeline\dashboard.py
```
或通过 `start.bat` 菜单选择 `[3]`

### 访问地址
```
http://localhost:8501
```

### 功能标签

#### 🎯 机会
- 查看 M12 扫描发现的交易机会
- 按优先级、市场、方向筛选
- 查看机会详情和交易计划

#### 📶 信号
- 查看所有处理过的信号
- 信号统计和分布
- 最近7天信号列表

#### 📊 模拟盘
- 查看当前持仓
- 历史交易记录
- 盈亏统计

#### 🧠 情绪面
- 恐贪指数趋势
- 北向资金流向
- 市场情绪分析

#### 🎛️ 工作流（重点！）

**新增功能：**

##### ⚙️ 调度器控制
- 🚀 启动调度器
- ⏸️ 停止调度器
- 🔄 重启调度器
- 实时查看调度器状态

##### 🎯 手动触发任务

**1. M12 市场扫描**（主动找机会）
```
价格数据 → 异动检测 → 反向溯因 → 趋势判断 → 机会生成
```
- 选择市场：A股/港股/美股
- 选择类型：
  - 盘中扫描：监控池内股票，快速发现异动
  - 盘前扫描：分析隔夜信号，制定开盘策略
  - 盘后扫描：全市场扫描，发现新机会
- 点击「执行 M12 扫描」
- 结果在「🎯 机会」标签查看

**2. 信号处理链**（处理用户信号）
```
用户信号 → M0收集 → M1解码 → M2存储 → M3判断 → M4行动计划
```
- 创建文本文件（新闻、公告、观察）
- 放入 `data/incoming/` 目录
- 点击「执行信号处理」
- 结果在「📶 信号」标签查看

---

## 🎮 两种核心功能对比

### M12 扫描 vs 信号处理

| 特性 | M12 扫描 | 信号处理 |
|------|----------|----------|
| **触发方式** | 主动扫描市场 | 被动处理用户输入 |
| **数据来源** | 实时行情数据 | 用户提供的文本 |
| **适用场景** | 发现价格异动 | 分析新闻/公告 |
| **输出** | 交易机会 | 信号评估+行动建议 |
| **使用时机** | 盘中/盘前/盘后 | 任何时候 |

**简单记忆：**
- **M12 扫描** = 看盘找机会（系统主动）
- **信号处理** = 分析消息（你告诉系统）

---

## 📝 典型使用流程

### 场景1：盘中发现机会
1. 打开 Dashboard
2. 进入「🎛️ 工作流」标签
3. 选择「A股 → 盘中扫描」
4. 点击「执行 M12 扫描」
5. 切换到「🎯 机会」查看结果

### 场景2：分析一条新闻
1. 创建文件 `data/incoming/news.txt`
2. 粘贴新闻内容
3. 打开 Dashboard
4. 进入「🎛️ 工作流」标签
5. 点击「执行信号处理」
6. 切换到「📶 信号」查看分析结果

### 场景3：自动化运行
1. 双击 `start.bat`
2. 选择 `[1]` 启动调度器
3. 系统自动执行所有任务：
   - 每30分钟处理新信号
   - 盘中每10分钟扫描异动
   - 每日自动复盘
4. 打开 Dashboard 查看结果

---

## 🔧 命令行快速参考

### 调度器
```bash
# 启动（后台）
python -m m7_scheduler.cli start --background

# 查看状态
python -m m7_scheduler.cli status

# 停止
python -m m7_scheduler.cli stop
```

### 手动触发
```bash
# M12 扫描
python -m m7_scheduler.cli run m12_a_share_scan

# 信号处理
python -m m7_scheduler.cli run signal_pipeline

# 价格更新
python -m m7_scheduler.cli run price_update
```

### Dashboard
```bash
# 启动
streamlit run pipeline\dashboard.py

# 指定端口
streamlit run pipeline\dashboard.py --server.port 8080
```

---

## ❓ 常见问题

### Q: Dashboard 和 bat 菜单有什么区别？
**A:** Dashboard 是网页界面，功能更强大，推荐使用。bat 菜单是命令行工具，适合快速操作。

### Q: 必须启动调度器吗？
**A:** 不是必须的。你可以：
- 只用 Dashboard 手动触发任务
- 或启动调度器实现自动化

### Q: M12 扫描没有发现机会？
**A:** 可能原因：
1. 不在交易时段
2. 股票池为空（检查 `data/stock_universe.json`）
3. 市场没有明显异动

### Q: 如何查看详细日志？
**A:** 
- 调度器日志: `data/logs/scheduler.log`
- 或在 bat 菜单选择 `[2] View real-time log`

---

## 📚 更多文档

- 完整用户手册: `docs/USER_GUIDE.md`
- 系统架构说明: `PROJECT_CONTEXT.md`
- 开发文档: `docs/` 目录

---

**祝交易顺利！** 🚀

有问题随时查看 `docs/USER_GUIDE.md` 或日志文件。
